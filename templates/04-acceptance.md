# 验收清单 - <需求标题>

## 验收项清单
| 编号 | 验收项 | 预期结果 | 测试类型 | 优先级 |
|---|---|---|---|---|
| A-001 | 待补充（可追溯 R-xx） | 待补充 | 单元/集成/E2E | P0/P1/P2 |

**测试类型说明**：单元 = 单类/单函数；集成 = 多模块/接口/DB；E2E = 端到端用户场景。**优先级**：P0 阻塞发布，P1 核心功能，P2 增强/边界。

## 验收计划与步骤
（**可测试性要求**：验收步骤须**可执行**——明确前置条件、操作与可观测结果；通过标准须**可断言**——可对应为测试中的明确判定，如返回值、状态码、数据变更；禁止仅用「功能正常」「体验良好」等不可验证表述。便于实现阶段按 TDD 先写失败测试再实现。**多验收项**：若有 A-002、A-003…，按同一结构复制下列 1～6 小节即可。**删除约束**：任何删除/清理操作必须慎重，只允许删除本验收/本用例**自己创建**的数据，或需求/验收中**明确标记为可删除的测试数据**，禁止删除存量或来源不明的数据。）

### A-001 验收计划与步骤

#### 1. 验收目标与范围
- **验收目标**：待补充（一句话描述本项要验证的结论）
- **测试类型**：待补充（单元 / 集成 / E2E）
- **依赖验收项**：无 / A-xxx（若依赖其他验收项通过或数据状态，在此列出）

#### 2. 前置条件（可复现的测试环境）
- **环境/配置**：
  1. 待补充（如：服务已启动、环境变量、配置开关）
  2. 待补充
- **数据准备**：
  - 待补充（如：给定用户 ID、给定订单状态、或「无」。**若为删除类验收项**：需在此处说明测试数据来源，是由本用例先创建，还是由用户明确提供「可删除数据」列表及标识。）
- **权限/身份**：待补充（如：已登录用户、特定角色、匿名）

#### 3. 验收步骤（每步可对应测试中的一个动作或断言点）
| 步骤 | 操作描述 | 操作对象/输入 | 可观测输出（便于断言） |
|:---:|---|----|----|
| 1 | 待补充 | 待补充 | 待补充（如：HTTP 状态码、响应字段、DB 变更） |
| 2 | 待补充 | 待补充 | 待补充 |
| 3 | 待补充 | 待补充 | 待补充 |

#### 4. 通过标准（须可编程断言，禁止模糊表述）
- **接口/返回值**：
  - 待补充（如：HTTP 200；或 `response.code === 0`；或返回体包含字段 `id`、`status`）
- **业务结果**：
  - 待补充（如：数据库表 `orders` 中 `id=xxx` 的 `status` 变为 `PAID`）
- **副作用/边界**：
  - 待补充（如：日志包含某关键字；或错误码为某值；若无则填「无」）

#### 5. 自动化测试支持（便于直接转化为用例）
- **建议断言**：
  1. 待补充（可直接写成测试断言，如：`expect(res.status).toBe(200)`）
  2. 待补充
- **测试数据**：待补充（构造请求体/查询参数的示例或引用；或「使用 2. 前置条件 中的数据」）
- **清理/回滚**：待补充（如：删除本用例创建的订单；或「无需清理」。**约束**：只允许删除本用例自己创建的数据，禁止删除存量或来源不明的数据）

#### 6. 失败处理
- 待补充（如：返回 4xx 时检查入参与权限；DB 未变更时检查事务与日志）

## 受影响功能验证
（回归范围：本需求改动可能影响的功能，需在验收时一并验证；每项应可转化为一条回归用例或验收项引用。）
| 受影响功能/模块 | 验证方式 | 可断言结果 |
|---|---|---|
| 待补充 | 待补充（如：调用接口 X、执行操作 Y） | 待补充（如：状态码、数据状态） |

## 数据库核对指引
（用于集成/端到端测试：接口调用后，用何种查询验证持久化结果与接口结果一致。）
- **核对表/集合**：待补充（如：`orders`、`inventory`）
- **查询示例**：待补充（如：`SELECT id, status FROM orders WHERE id = ?` 与响应体比对）
- **预期一致字段**：待补充（如：`status`、`updated_at`）

## 澄清补充
<!-- CLARIFICATIONS:START -->
- 无
<!-- CLARIFICATIONS:END -->

## 依赖签名（哈希由外部流程更新；详见 reference/spec-agent-reference.md）
<!-- DEPENDENCY-SIGNATURE:START -->
- analysis: 
- prd: 
- tech: 
<!-- DEPENDENCY-SIGNATURE:END -->

## 修订记录
| 修订日期 | 修订人 | 修订内容摘要 |
|---|:---:|---|
| YYYY-MM-DD | 初始化 | 初始创建 |

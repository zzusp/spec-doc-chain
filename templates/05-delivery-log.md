# 交付日志 - <需求标题>

> 说明：本日志用于记录「开发 → 验收 → 不通过 → 修复 → 再验收 … → 通过」全流程证据与决策。  
> 建议存放路径：`spec/<YYYY-MM-DD>/<需求名称>/05-delivery-log.md`（与 01-04-00 同目录）。
>
> **上下文长度约束（重要）**：本日志面向“可追溯 + 可复现”，不是原始输出仓库。  
> - 单轮记录尽量控制在 **~100 行以内**（更少更好）  
> - **只贴关键片段**（错误栈/关键响应字段/关键日志行），其余用「命令 + 关键参数 + 截断摘要」表示  
> - 不要在日志里反复粘贴整份 `04-acceptance.md`、整段长响应体、或完整控制台输出

## 元信息
- **需求目录**：`spec/<YYYY-MM-DD>/<需求名称>/`
- **开始时间**：YYYY-MM-DD HH:mm
- **结束时间**：YYYY-MM-DD HH:mm（通过后填写）
- **环境信息**：
  - OS：待补充
  - 运行时/版本：待补充（如 Node/Python/Java）
  - 关键依赖：待补充
- **范围声明**：
  - **实现依据**：`02-prd.md`、`03-tech.md`
  - **验收依据**：`04-acceptance.md`（A-xxx）

## 变更请求记录（用户反馈偏差/调整时追加）
| 编号 | 时间 | 提出方 | 诉求/调整点（尽量引用原话） | 影响范围（PRD/Tech/Acceptance/Code） | 处理状态 |
|---:|---|---|---|---|---|
| CR-001 | YYYY-MM-DD HH:mm | 用户/团队 | 待补充 | PRD/验收/代码 | 待分析/已对齐文档/实现中/已交付 |

## 验收总览（逐轮更新）
| 轮次 | 验收范围 | 结论 | 不通过原因摘要 | 证据（命令/截图/日志） | 修复提交/变更摘要 |
|---:|---|---|---|---|---|
| 1 | A-xxx… | 不通过/通过 | 待补充 | 待补充 | 待补充 |

## 证据记录规范（避免日志膨胀）
- **命令优先**：记录可复现命令（例如 `npm test` / `pnpm test` / `pytest -q` / `curl ...`），并写明关键参数与运行目录。
- **输出只保留“关键行”**：最多保留 10～30 行关键输出，其余用 `...（已截断）` 表示，并在摘要中说明“如何再次获取完整输出”。
- **截图/附件用引用**：用路径或标识引用（如「见截图：xxx.png」「见日志关键字：REQUEST_ID=...」），不要在正文嵌入超长内容。

## 轮次详情

### Round 1（YYYY-MM-DD HH:mm）
#### 1. 本轮目标
- **验收项**：A-xxx（可多项）
- **本轮假设/约束**：待补充（如：使用本地 mock；无外网）

#### 2. 开发实现记录（RED → GREEN → REFACTOR）
- **RED（先写失败测试）**：
  - 新增/修改测试：待补充（文件路径 / 关键断言）
  - 运行结果：失败（贴关键错误）
- **GREEN（最小实现）**：
  - 关键改动：待补充（文件路径 / 关键逻辑）
  - 回归影响评估：待补充
- **REFACTOR（可选）**：
  - 重构点：待补充（保持测试通过）

#### 3. 验收执行（必须真实执行）
- **执行步骤**：引用 `04-acceptance.md` 对应小节；必要时补充本轮实际命令
- **真实观测**：
  - 关键输出：待补充（状态码/返回值/DB 变更/日志关键字）
  - 失败现象（若不通过）：待补充
- **本轮结论**：通过 / 不通过

#### 4. 不通过分析与根因（仅在不通过时填写）
- **根因定位**：待补充（定位路径、相关模块）
- **为何此前未覆盖**：待补充（测试缺失/边界遗漏/误解需求）

#### 5. 修复方案与验证（仅在不通过时填写）
- **修复方案**：待补充（与 PRD/技术设计一致；若需变更文档，在此记录并同步更新）
- **修复后验证**：
  - 复跑测试：待补充（命令与结果）
  - 复跑验收：待补充（命令与结果）

## 验收项通过情况（最终必须全绿）
| 验收项 | 状态 | 最近一次通过轮次 | 证据 |
|---|---|---:|---|
| A-001 | 未开始/不通过/通过 | - | 待补充 |

## 交付结论（仅在全部通过后填写）
- **最终结论**：通过
- **通过依据**：列出所有 A-xxx 的可验证证据（命令输出/日志/截图/数据核对）
- **已解决问题清单**：待补充
- **已知限制（非阻塞）**：待补充（如无则写“无”）

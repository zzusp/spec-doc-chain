# 交付日志 - <需求标题>

> 说明：本日志用于记录「开发 → 验收 → 不通过 → 修复 → 再验收 … → 通过」全流程证据与决策。  
> 建议存放路径：`spec/<YYYY-MM-DD>/<需求名称>/05-delivery-log.md`（与 01-04-00 同目录）。

## 首页仪表盘（审阅者优先看这里）
- **需求目录**：`spec/<YYYY-MM-DD>/<需求名称>/`
- **开始时间**：YYYY-MM-DD HH:mm
- **结束时间**：YYYY-MM-DD HH:mm（全部通过后填写）
- **当前进度**：Round N（最新一轮）
- **当前结论**：通过 / 不通过 / 阻塞
- **当前卡点（如不通过/阻塞）**：A-xxx + 一句话原因
- **下一步**：一句话下一动作（继续验收/进入修复/环境自救/等待用户侧不可替代条件）

## 规则（主日志只保留索引相关）
- **可读性目标**：让不了解上下文的人在 3 分钟内回答：卡哪项？根因？怎么复验？
- **轮次记录细则**：放在每轮详情文件（见 `templates/05-delivery-round.md`），避免在主日志重复。

## 环境信息
- OS：待补充
- 运行时/版本：待补充（如 Node/Python/Java）
- 关键依赖：待补充

## 依据（不在日志里重复粘贴正文）
- **实现依据**：`02-prd.md`、`03-tech.md`
- **验收依据**：`04-acceptance.md`（A-xxx）
- **澄清约束（如有）**：`00-clarifications.md`（仅已确认项）

## 验收项状态总表（固定表，每轮结束必须更新）

> 用一张表同时回答三件事：**状态**、**证据（看得见）**、**如何复验**。  
> 规则：
> - 表内“证据”必须是**可见产物**（接口关键响应字段/错误码与 message、SQL 查询结果关键行、Before/After 对比），**不能只写脚本名或脚本变量名**。
> - 若你用脚本跑出来了计数/变量（如 `relation_active_count`），必须在表内用 **SQL + 结果关键行**改写成用户可理解的证据表达。

| 验收项 | 简述 | 优先级 | 状态 | 最近轮次 | 复验命令/入口 | 可见证据（Observed，关键片段） | DB 证据（SQL + 结果关键行） | 备注/阻塞 |
|---|---|---|---|---:|---|---|---|---|
| A-001 | 待补充 | P0/P1/P2 | 未开始/不通过/通过/阻塞/MOCK-BLOCKED | - | 待补充 | 待补充 | 待补充 | 待补充 |

## 变更请求记录（用户反馈偏差/调整时追加）
| 编号 | 时间 | 提出方 | 诉求/调整点（尽量引用原话） | 影响范围（PRD/Tech/Acceptance/Code） | 处理状态 |
|---:|---|---|---|---|---|
| CR-001 | YYYY-MM-DD HH:mm | 用户/团队 | 待补充 | PRD/验收/代码 | 待分析/已对齐文档/实现中/已交付 |

## 轮次索引（总览 + 详情入口，逐轮更新）

> 作用：一张表同时承担“轮次总览”与“Round 详情入口”，避免两张表重复维护产生不一致。  
> 推荐：每轮详情写入独立文件（模板见 `templates/05-delivery-round.md`），主日志只保留本表与状态总表。
>
> **硬规则**：每当本表新增 Round N，必须同时产出对应的详情文件（例如 `delivery-rounds/round-<NN>.md`），不得只更新本表。

| Round | 结论 | 覆盖验收项 | 详情文件 | 一句话根因/阻塞 | 证据引用（Round/A-xxx 证据包） | 变更摘要（文件/提交） | 一句话下一步 |
|---:|---|---|---|---|---|---|---|
| 1 | 不通过/通过/阻塞 | A-xxx… | `delivery-rounds/round-01.md` | 待补充 | 待补充 | 待补充 | 待补充 |

## 归档（可选，控制文件长度）
> 若已使用 `delivery-rounds/` 拆分轮次详情，通常无需在本文件再做归档；只需保证 round 文件可追溯即可。  
> 若未拆分，可将较早轮次的冗长细节搬到此处，但主文件仍应保持“索引/摘要优先”。
- Archived Rounds: Round 1～Round N-3（可选）

## 交付结论（仅在全部验收项于本流程内执行并通过后填写）
- **约束**：仅当所有 A-xxx 的「执行 + 观测 + 判定 + 证据」均在本流程内完成、且交付后不存在任何“需用户再执行某操作才能完成的验证”时，方可填写。若存在“需用户跑脚本/设环境/确认”等才可验证的项，不得写交付结论。
- **最终结论**：通过
- **通过依据**：列出所有 A-xxx 在本流程内执行所得的可验证证据（命令与关键输出/日志/数据核对）
- **已解决问题清单**：待补充
- **已知限制（非阻塞）**：待补充（如无则写“无”）
